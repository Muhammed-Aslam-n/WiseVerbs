import 'package:flutter/material.dart';

class ShowSimplePopup extends StatelessWidget {
  const ShowSimplePopup({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
